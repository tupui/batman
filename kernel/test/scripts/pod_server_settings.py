# settings for running the pod decomposition and prediction as a separate application
pod_server = {
# Server hostname with port
    'port'   : 8000,
# Python executable that runs the server
    'python' : '/usr/local/python2.6.5/bin/python',
}
